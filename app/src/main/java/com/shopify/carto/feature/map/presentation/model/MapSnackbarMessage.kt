package com.shopify.carto.feature.map.presentation.model

enum class MapSnackbarMessage {
    LocationUnavailable,
    LocationSelectedWithMissingDetails,
    SearchFailed,
    AddressResolveFailed,
    Unknown,
}
