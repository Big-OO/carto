<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Carto" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/background_blue</item>
        <item name="android:statusBarColor">@color/background_blue</item>
        <item name="android:navigationBarColor">@color/background_blue</item>
    </style>

    <!-- Splash Screen theme (Android 12+ SplashScreen API) -->
    <style name="Theme.Carto.Splash" parent="Theme.Carto">
        <item name="android:windowSplashScreenBackground">@color/navy_deep</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="android:windowSplashScreenBrandingImage">@drawable/ic_launcher_foreground</item>
    </style>

</resources>