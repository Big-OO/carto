query SearchProducts($query: String!, $first: Int!) {
  search(query: $query, first: $first, types: [PRODUCT], prefix: LAST) {
    nodes {
      ... on Product {
        id
        title
      }
    }
  }
}
